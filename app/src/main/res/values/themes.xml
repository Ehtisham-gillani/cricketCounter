<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.CricketCounter" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/cricket_green</item>
        <item name="colorPrimaryVariant">@color/cricket_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/cricket_gold</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:colorBackground">@color/surface_bg</item>
        <item name="android:statusBarColor">@color/cricket_green_dark</item>
    </style>

    <style name="Theme.CricketCounter" parent="Base.Theme.CricketCounter" />

    <!-- Run buttons — large, thumb-friendly -->
    <style name="RunButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:minHeight">56dp</item>
        <item name="elevation">4dp</item>
    </style>

    <!-- Extra / Wicket chip buttons -->
    <style name="ChipButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">20dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
    </style>

    <!-- Utility / secondary buttons (SWAP, UNDO) -->
    <style name="UtilButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">10dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:minHeight">48dp</item>
    </style>

</resources>